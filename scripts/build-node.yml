---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: '6'

inputs:
- name: operator

run:
  path: /bin/bash
  args:
    - -c
    - |
      P=$pwd
      echo $P
      ls
      npm install
      npm run test
